// Create a test for the ExerciseList Page to check the Simple list, page count, filters, sorts, pagination and the colors of the label

import { meSuperadmin } from "../intercepts/login";
import { exerciseList } from "../intercepts/exerciseList";